# Charts factory using rechart
- How works this component?
  - This component might be created  passing a property of kind like:
  ```
  interface Collection {
  collection: {
    labelX: string;
    dataKey: number;
  }[];
  ```
}
